"use client";
// Components ---------------------------------------------------------------------------------------------------------------------------------------------------------- [ IMPORTS ]
import { ThemeProvider } from "next-themes";
// Dependencies -------------------------------------------------------------------------------------------------------------------------------------------------------- [ IMPORTS ]
import { setCookie } from "cookies-next";
// Hooks --------------------------------------------------------------------------------------------------------------------------------------------------------------- [ IMPORTS ]
import { useTheme } from "next-themes";
import { useEffect } from "react";
// Types --------------------------------------------------------------------------------------------------------------------------------------------------------------- [ IMPORTS ]
import type { ThemeProviderProps } from "next-themes/dist/types";
// Theme ------------------------------------------------------------------------------------------------------------------------------------------------------ [ HELPER COMPONENT ]
const Theme = ({ children, ...props }: ThemeProviderProps): React.JSX.Element => {
    // Hooks --------------------------------------------------------------------------------------------------------------------------------------------------------- [ COMPONENT ]
    const { theme } = useTheme();
    // Effects ------------------------------------------------------------------------------------------------------------------------------------------------------- [ COMPONENT ]
    useEffect(() => { setCookie(`theme`, theme || `dark`, { expires: new Date(Date.now() + 1000 * 60 * 60 * 24 * 365) , path: `/` }) }, [ theme ]);
    // JSX ----------------------------------------------------------------------------------------------------------------------------------------------------------- [ COMPONENT ]
    return <ThemeProvider enableColorScheme { ...props }>
        { children }
    </ThemeProvider>;
};
// Default Export ------------------------------------------------------------------------------------------------------------------------------------------------------ [ EXPORTS ]
export default Theme;