type TConfiguration = {
    locales: string[],
    localesFallback: string,
    projectName: string,
    themes: string[],
    themesFallback: string,
    version: string
};

export default TConfiguration;